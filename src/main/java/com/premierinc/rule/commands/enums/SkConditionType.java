package com.premierinc.rule.commands.enums;

import static jdk.nashorn.internal.parser.TokenType.ELSE;
import static jdk.nashorn.internal.parser.TokenType.IF;

/**
 *
 */
public enum SkConditionType {
	IF, THEN, ELSE, UNKNOWN;
}
